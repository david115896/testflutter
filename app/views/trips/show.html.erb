<p id="notice"><%= notice %></p>

<%= @trip.city%>

<% @trip.list_trip.each do |line trip|%>
<%= line.trip.activity_id%>

<%end%>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>